version: '3'
services:
  api-server:
    image: ems-backend-api
    ports:
      - '3000:3000'
    depends_on:
      - mongo

  mongo:
    image: mongo:3.6.21-xenial
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
    driver: local
